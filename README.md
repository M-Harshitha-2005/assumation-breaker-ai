# assumation-breaker-ai
This Streamlit app implements an Assumption Breaker AI using Google Gemini. It supports user login/signup with hashed passwords, manages sessions and chat history, takes a problem statement, analyzes hidden assumptions using Gemini with retry logic, displays results, tracks session stats, and allows secure logout.
